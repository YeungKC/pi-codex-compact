name: Publish package

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

jobs:
  version-check:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.version.outputs.changed }}
    steps:
      - name: Check out source
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Check package version
        id: version
        run: |
          current_version="$(node -p "require('./package.json').version")"
          if git cat-file -e HEAD^:package.json 2>/dev/null; then
            previous_version="$(git show HEAD^:package.json | node -e 'process.stdout.write(JSON.parse(require("fs").readFileSync(0, "utf8")).version)')"
          else
            previous_version=""
          fi

          echo "Current package version: $current_version"
          echo "Previous package version: ${previous_version:-<none>}"
          if [ "$current_version" != "$previous_version" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

  publish:
    needs: version-check
    if: needs.version-check.outputs.changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install test dependencies
        run: bun install --no-progress

      - name: Run tests
        run: bun test

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Publish to npm with provenance
        run: npm publish --provenance --access public
